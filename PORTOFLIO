<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Olivier R. — Portfolio</title>
  <meta name="description" content="Olivier R — Full-stack dev. Works with JS, TS, Haskell. Building on Cardano & Polkadot." />
  <!-- Simple pleasant fonts from Google (optional) -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0b1020;
      --card:#0f1724;
      --accent:#00b4d8;
      --muted:#9aa4b2;
      --glass: rgba(255,255,255,0.03);
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:Inter,system-ui,Segoe UI,Roboto,"Helvetica Neue",Arial;
      background:linear-gradient(180deg,#041022 0%, var(--bg) 100%);
      color:#e6eef6;
      -webkit-font-smoothing:antialiased;
      line-height:1.45;
    }
    .container{max-width:1000px;margin:32px auto;padding:24px;}
    header{display:flex;align-items:center;justify-content:space-between;gap:16px}
    .brand{display:flex;gap:12px;align-items:center}
    .logo{
      width:56px;height:56px;border-radius:12px;background:var(--glass);display:flex;align-items:center;justify-content:center;font-weight:700;color:var(--accent);
      font-size:20px;box-shadow:0 6px 18px rgba(0,0,0,0.5);
    }
    h1{margin:0;font-size:20px}
    p.lead{margin:6px 0 0;color:var(--muted);font-size:14px}

    .grid{
      display:grid;
      grid-template-columns:1fr 360px;
      gap:20px;
      margin-top:20px;
    }

    main{background:transparent;padding:18px;border-radius:12px}
    .card{background:var(--card);padding:18px;border-radius:12px;box-shadow:0 8px 30px rgba(2,6,23,0.6);}

    .about{display:flex;gap:18px;align-items:flex-start}
    .avatar{width:96px;height:96px;border-radius:12px;background:linear-gradient(135deg,var(--accent),#7a5cff);display:flex;align-items:center;justify-content:center;font-weight:700;color:white;font-size:28px}
    .skills{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
    .tag{background:rgba(255,255,255,0.04);padding:6px 10px;border-radius:999px;font-size:13px;color:var(--muted)}

    .projects{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px;margin-top:14px}
    .project{background:var(--glass);padding:12px;border-radius:8px}
    .project h3{margin:0 0 6px;font-size:16px}
    .project p{margin:0;color:var(--muted);font-size:13px}

    aside .card{position:sticky;top:20px}
    form{display:flex;flex-direction:column;gap:10px}
    input,textarea,button{
      background:transparent;border:1px solid rgba(255,255,255,0.06);padding:10px;border-radius:8px;color:inherit;font-size:14px;
    }
    textarea{min-height:110px;resize:vertical}
    button{
      background:linear-gradient(90deg,var(--accent),#7a5cff);border:0;color:#061226;padding:10px 14px;font-weight:600;cursor:pointer;border-radius:10px;
    }
    footer{margin-top:28px;padding:12px;color:var(--muted);font-size:13px;text-align:center}

    /* Responsive */
    @media (max-width:920px){
      .grid{grid-template-columns:1fr}
      .avatar{width:72px;height:72px}
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="brand">
        <div class="logo">OR</div>
        <div>
          <h1>Olivier Rishi Matabaro</h1>
          <p class="lead">Full-stack dev • Cardano & Polkadot enthusiast • JS / TS / Haskell</p>
        </div>
      </div>

      <nav aria-label="main">
        <a href="#projects" style="color:var(--muted);text-decoration:none;margin-right:12px">Projects</a>
        <a href="#contact" style="color:var(--muted);text-decoration:none">Contact</a>
      </nav>
    </header>

    <div class="grid" role="main">
      <main>
        <section class="card about" aria-labelledby="about-title">
          <div class="avatar" aria-hidden="true">OR</div>
          <div>
            <h2 id="about-title">About me</h2>
            <p style="color:var(--muted)">I’m a junior full-stack developer focused on building practical apps and learning functional programming with Haskell and Plutus. I enjoy blockchain projects and open-source collaboration.</p>

            <div class="skills" aria-hidden="false" role="list">
              <span class="tag">JavaScript</span>
              <span class="tag">TypeScript</span>
              <span class="tag">Haskell</span>
              <span class="tag">React</span>
              <span class="tag">Cardano</span>
              <span class="tag">Polkadot</span>
            </div>
          </div>
        </section>

        <section id="projects" class="card" aria-labelledby="projects-title" style="margin-top:16px">
          <h2 id="projects-title">Selected projects</h2>
          <div class="projects" role="list">
            <article class="project" role="listitem">
              <h3>Plutus TX Template</h3>
              <p>Template for Plutus smart contract development — includes build scripts and examples.</p>
            </article>

            <article class="project" role="listitem">
              <h3>Web3 Landing</h3>
              <p>Responsive landing page for Web3 projects with social links & simple contact form.</p>
            </article>

            <article class="project" role="listitem">
              <h3>CLI Tools</h3>
              <p>Small Python & Node.js scripts to automate common tasks (deploy, lint, tests).</p>
            </article>
          </div>
        </section>

        <section class="card" style="margin-top:16px">
          <h2>Why this repo?</h2>
          <p style="color:var(--muted)">This single-file portfolio is easy to host (GitHub Pages) and to adapt. Add your own screenshots, update the projects list, and link to source repos for each project.</p>
        </section>
      </main>

      <aside>
        <div class="card" id="contact" aria-labelledby="contact-title">
          <h2 id="contact-title">Contact me</h2>
          <!-- Replace the form action with your Formspree / Netlify form endpoint -->
          <form id="contactForm" action="https://formspree.io/f/your-id" method="POST" novalidate>
            <label for="name" style="font-size:13px;color:var(--muted)">Name</label>
            <input id="name" name="name" required placeholder="Your name" />

            <label for="email" style="font-size:13px;color:var(--muted)">Email</label>
            <input id="email" name="email" type="email" required placeholder="you@example.com" />

            <label for="message" style="font-size:13px;color:var(--muted)">Message</label>
            <textarea id="message" name="message" required placeholder="Write a short message"></textarea>

            <input type="hidden" name="_subject" value="New contact from portfolio" />
            <button type="submit">Send message</button>
            <p id="status" style="color:var(--muted);font-size:13px;margin:6px 0 0"></p>
          </form>

          <hr style="margin:16px 0;border:none;border-top:1px solid rgba(255,255,255,0.03)"/>

          <p style="color:var(--muted);font-size:13px">
            Or find me on:
            <br><a href="https://github.com/Olivierrishimatabaro" style="color:var(--accent)">GitHub</a> • <a href="#" style="color:var(--accent)">LinkedIn</a> • <a href="#" style="color:var(--accent)">X</a>
          </p>
        </div>
      </aside>
    </div>

    <footer>
      Built with ❤️ — clone, customize, and host on GitHub Pages (Settings → Pages).
    </footer>
  </div>

  <script>
    // Small client-side validation + send with fetch (fallback to normal POST)
    (function(){
      const form = document.getElementById('contactForm');
      const status = document.getElementById('status');

      form.addEventListener('submit', async function(e){
        e.preventDefault();
        status.textContent = 'Sending...';

        const data = new FormData(form);

        // Basic validation
        const email = data.get('email') || '';
        if(!/^\S+@\S+\.\S+$/.test(email)){
          status.textContent = 'Please enter a valid email.';
          return;
        }

        try{
          // Try fetch to Formspree (CORS allowed)
          const resp = await fetch(form.action, {
            method: 'POST',
            body: data,
            headers: { 'Accept': 'application/json' }
          });
          if(resp.ok){
            status.textContent = 'Message sent — thank you!';
            form.reset();
          } else {
            const resJson = await resp.json().catch(()=>null);
            status.textContent = (resJson && resJson.error) ? resJson.error : 'Sending failed — try again later.';
          }
        } catch(err){
          // fallback: submit normally (this will navigate away)
          status.textContent = 'Network issue — falling back to default submit.';
          form.removeEventListener('submit', arguments.callee);
          form.submit();
        }
      });
    })();
  </script>
</body>
</html>
